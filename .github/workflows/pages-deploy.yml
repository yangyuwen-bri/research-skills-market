name: GitHub Pages Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Rebuild showcase page
        run: |
          python3 scripts/publish_market_showcase.py --curated data/curated_market_v2.json --output docs/index.html

      - name: Check optional pages admin token
        id: check_pages_token
        run: |
          if [ -n "${{ secrets.PAGES_ADMIN_TOKEN }}" ]; then
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_token=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Configure GitHub Pages (auto enable)
        id: configure_pages_with_pat
        if: ${{ steps.check_pages_token.outputs.has_token == 'true' }}
        uses: actions/configure-pages@v5
        with:
          token: ${{ secrets.PAGES_ADMIN_TOKEN }}
          enablement: true

      - name: Configure GitHub Pages
        id: configure_pages
        if: ${{ steps.check_pages_token.outputs.has_token == 'false' || steps.configure_pages_with_pat.outcome != 'success' }}
        uses: actions/configure-pages@v5

      - name: Upload site
        if: ${{ steps.configure_pages_with_pat.outcome == 'success' || steps.configure_pages.outcome == 'success' }}
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
